# ── Vercel Environment Variables ──────────────────────────────────────────────
# The URL of your GCP VM FastAPI server
NEXT_PUBLIC_API_URL=http://YOUR_GCP_VM_EXTERNAL_IP:8000

# The bearer token (must match API_BEARER_TOKEN on the GCP VM)
NEXT_PUBLIC_API_BEARER_TOKEN=your_super_secret_token_here
